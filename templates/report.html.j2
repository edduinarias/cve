<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Reporte CVE-2025-24813</title>
  <style>
    body { font-family: Arial, sans-serif; }
    table { border-collapse: collapse; width: 100%; }
    th, td { border: 1px solid #999; padding: 6px; text-align: left; }
    th { background-color: #eee; }
    .vulnerable { background-color: #ffdddd; }
    .no-vulnerable { background-color: #ddffdd; }
  </style>
</head>
<body>
  <h2>Reporte Consolidado - CVE-2025-24813</h2>
  <table>
    <tr>
      <th>Host</th>
      <th>Versión Tomcat</th>
      <th>Estado</th>
      <th>Salida catalina.sh</th>
    </tr>
    {% for h in hosts_summary %}
    <tr class="{{ 'vulnerable' if h.vulnerability_status == 'Vulnerable' else 'no-vulnerable' }}">
      <td>{{ h.host }}</td>
      <td>{{ h.tomcat_pkg_version_display }}</td>
      <td>{{ h.vulnerability_status }}</td>
      <td><pre>{{ h.catalina_raw }}</pre></td>
    </tr>
    {% endfor %}
  </table>
  <p><i>Generado automáticamente con Ansible</i></p>
</body>
</html>
